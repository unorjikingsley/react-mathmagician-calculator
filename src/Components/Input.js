import React from 'react';

const Input = () => (
  <div className="input">
    <form>
      <input type="text" placeholder="0" aria-label="Calculator Input" />
    </form>
  </div>
);

export default Input;
